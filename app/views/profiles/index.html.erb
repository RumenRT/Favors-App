

<h1 id= "nameprofile"> <%= @profiles.first_name %>'s Profile</h1>

  <div id="mainprofile">
    <ul class="list-group" id="listgroup">
      <li class="list-group-item"><p>First Name:</p><%= @profiles.first_name %></li>
      <li class="list-group-item"><p>Age:</p><%= @profiles.age %></li>
      <li class="list-group-item"><p>Favors completed:</p><%= @profiles.favors_completed %></li>
      <li class="list-group-item"><p>Favors offered:</p><%= @profiles.favors_offered %></li>
    </ul>
      <form class="form-inline my-2 my-lg-0 col-md-1" id="searchfavor">
      <input class="form-control mr-sm-2" type="search" placeholder="Quick Favor" aria-label="Search">
      <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Request help</button>
    </form>
  </div>

<%# <hr> %>
<div id="favorlist">
<h4> Your Open-Favors </h4>

<table class="table">
  <thead>
    <tr>
      <th scope="col">Date</th>
      <th scope="col">Category</th>
      <th scope="col">Location</th>

    </tr>
  </thead>
  <tbody>
    <%# <tr>
    <%# Instance variable @favors iterate over (favors.each do |favor|
    favor.description 
    favor.Category %>
    <%# </tr> %> 
    <tr>
      <th scope="row">@favors_offered.date </th>
      <td>@favors_offered.Category</td>
      <td>@favors_offered.location</td>
    </tr>
    <tr>
      <th scope="row">@favors_offered.date </th>
      <td>@favors_offered.Category</td>
      <td>@favors_offered.location</td>

 
 


    </tr>
  </tbody>
</table>
</div>
<%# <hr> %>
<% @favors.each do |favor|%>
<table>
<tr> <%= favor.id %> </tr>
<tr> <%= favor.description %> </tr>
   
<% end %>








<body>
    <footer class="navbar navbar-fixed-bottom navbar-inverse" style="background-color: teal">
      <div class="container" id="navbarprof">
        <nav>
          <ul class="nav navbar-nav navbar-center" style="background-color: teal;" id="navbar">
            <li><%= link_to "Home",   './profiles#index' %> </li>
            <li><%= link_to "Map", './favors#index', id: "logo" %> </li>
            <li><%= link_to "New Favor", './favors/new', id: "logo" %> </li>
            <li><a rel="nofollow" data-method="delete" href="/users/sign_out">Log Out</a></li>
          </ul>
        </nav>
      </div>
    </footer>
    <div class="container">
    </div>
  </body>

 